import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    headerTitle: {
        width: "100%",
        backgroundColor: "#eeeeee",
        padding: 22,
        alignItems: "center",
    },
    headerText: {
        fontSize: 28,
        fontWeight: "bold",
    },
});

export const headerTitle = styles.headerTitle;
export const headerText = styles.headerText;